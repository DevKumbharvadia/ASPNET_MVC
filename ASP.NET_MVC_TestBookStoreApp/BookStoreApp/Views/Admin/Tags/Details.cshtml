@model BookStoreApp.Models.Domain.Tag

@{
    ViewData["Title"] = "Tag Details"; // Set the page title
}

<h2>Tag Details</h2>

<div>
    <h4>@Model.Name</h4>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
